import AgentesLanding from "@/components/landing/AgentesLanding";

// La home es la landing B2B para agentes. /agentes es un alias con el mismo
// contenido; ambas declaran canonical "/" en su layout.
export default function HomePage() {
  return <AgentesLanding />;
}
